import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#cb69ab',
      light: 'skyblue',
    },
    secondary: { main: '#0cb6bf' },
    otherColor: { main: '#999' },
  },
});
